# service

Micronaut application and composition root of the modular monolith.

This module may compose the other modules and hold runtime configuration. Domain logic should remain in `modules/*`.

Documentation generated by Codex.
