# downloader

Module for downloading Markdown content from the source project.

It currently registers a handler for `DownloadMarkdownPageQuery`. Through `MarkdownPageDownloader`, the handler downloads remote content with HTTP GET, prepares a safe `.md` file name, and returns `DownloadedMarkdownPage` through the query bus.

Documentation generated by Codex.
